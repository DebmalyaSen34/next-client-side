/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.cnn.com', 'b.zmtcdn.com', 'www.eatthis.com', 'www.holidify.com', 'i0.wp.com']
    }
};

export default nextConfig;
